@model Book

<div class="row">
    <div class="col-lg-5">
        <!-- Product Details Slider Big Image-->
        <div>
            <div class="single-slide">
                <img src="~/assets/image/products/@Model.BookImages.FirstOrDefault(x=>x.PosterStatus==true)?.Image" alt="">
            </div>
        </div>
        <!-- Product Details Slider Nav -->
    </div>
    <div class="col-lg-7 mt--30 mt-lg--30">
        <div class="product-details-info pl-lg--30 ">
            <p class="tag-block">
                Tags:
                @for (int i = 0; i < Model.BookTags.Count; i++)
                {
                    <a href="#">@Model.BookTags[i].Tag.Name</a>
                    @(i!=Model.BookTags.Count-1?",":"")
                }
            </p>
            <h3 class="product-title">@Model.Name</h3>
            <ul class="list-unstyled">
                <li>Genre: <a href="#" class="list-value font-weight-bold genre-name"> @Model.Genre.Name</a></li>
                <li>Product Code: <span class="list-value"> @Model.Code</span></li>
                <li>Availability: <span class="list-value"> @(Model.IsAvailable?"In Stock":"Out Stock")</span></li>
            </ul>
            <div class="price-block">
                <span class="price-new">£ @(Model.SalePrice-Model.DiscountPrice)</span>
                @if (Model.DiscountPrice > 0)
                {
                    <del class="price-old">£ @Model.SalePrice</del>
                }
            </div>
            <div class="rating-widget">
                <div class="rating-block">
                    @for (int i = 1; i <= 5; i++)
                    {
                        if (i <= Model.Rate)
                        {
                            <span class="fas fa-star star_on"></span>
                        }
                        else
                        {
                            <span class="fas fa-star "></span>

                        }
                    }
                </div>
                <div class="review-widget">
                    <a href="#">(1 Reviews)</a> <span>|</span>
                    <a href="#">Write a review</a>
                </div>
            </div>
            <article class="product-details-article">
                <h4 class="sr-only">Product Summery</h4>
                <p>
                   @Model.InfoText
                </p>
            </article>
            <div class="add-to-cart-row">
                <div class="count-input-block">
                    <span class="widget-label">Qty</span>
                    <input type="number" class="form-control text-center" value="1">
                </div>
                <div class="add-cart-btn">
                    <a href="#" class="btn btn-outlined--primary">
                        <span class="plus-icon">+</span>Add to Cart
                    </a>
                </div>
            </div>
            <div class="compare-wishlist-row">
                <a href="#" class="add-link"><i class="fas fa-heart"></i>Add to Wish List</a>
                <a href="#" class="add-link"><i class="fas fa-random"></i>Add to Compare</a>
            </div>
        </div>
    </div>
</div>